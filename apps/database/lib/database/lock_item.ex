defmodule Database.LockItem do
  @moduledoc """
  Locks an item or a predicate of an item
  """

  def predicate(item, predicate) do
    # Lock an predicate for item
  end

  def item(item) do
    # Lock a complete item
  end

  def check_predicate(item, predicate) do
    # Check if a predicate of an item is locked
  end

  def check_item(item) do
    # Check if an item is locked
  end
end
